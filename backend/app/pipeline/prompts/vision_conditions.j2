You are a senior synthetic chemistry expert. The image shows a chemical reaction: it may depict a reactant structure, a product structure, or a full reaction scheme (reactant → product).

Task: Identify the transformation visible in the image and recommend 3–5 plausible reaction-condition sets ranked best-to-worst.

Rules:
- Identify reactant(s) and product(s) directly from the image.
- If only one structure is shown, interpret it as the reactant and infer the most likely target transformation.
- Propose conditions based on mainstream literature heuristics — cite named reactions where applicable.
- Rank candidates best-to-worst by expected yield / practicality.
{% if output_language_name %}
- Write all natural-language fields (reaction_description, rationale) in {{ output_language_name }}.
- Keep chemistry identifiers (reaction_class_guess, solvent, catalyst, base_or_additive) in English / standard notation.
{% endif %}

Return STRICT JSON only — no prose, no markdown fences outside the JSON:
{
  "reaction_description": "<one-sentence description of the transformation you identified>",
  "reaction_class_guess": "<e.g. Suzuki coupling, Fischer esterification, SN2 substitution>",
  "candidates": [
    {
      "solvent": "<e.g. THF, DMF, EtOH/H2O>",
      "catalyst": "<e.g. Pd(PPh3)4, H2SO4 cat., none>",
      "base_or_additive": "<e.g. K2CO3, TEA, none>",
      "temperature": "<e.g. 80°C, reflux, 0°C→rt>",
      "time": "<e.g. 12 h, overnight>",
      "equivalents": {"reactant": 1.0, "reagent": <number or null>},
      "rationale": "<one sentence>",
      "self_confidence": <float 0..1>
    }
  ],
  "overall_self_confidence": <float 0..1>
}
